const state = {
	user: null
};

const mutations = {

};

const actions = {

};

const getters = {
	profile(state) {
		return state.user;
	}
};

export default {
	state,
	mutations,
	actions,
	getters
}
